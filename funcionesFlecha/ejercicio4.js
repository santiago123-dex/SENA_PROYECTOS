let base = Number(prompt("Ingrese la base del rectángulo"));
let altura = Number(prompt("Ingrese la altura del rectángulo"));

const calcularArea = (b, h) => b * h;

alert(`El área del rectángulo es ${calcularArea(base, altura)}.`);
